{
  "name": "App Dev (Compose, target=dev)",
  "dockerComposeFile": "docker-compose.yml",
  "service": "mumble",
  "workspaceFolder": "/home/node",
  "remoteUser": "node",
  "overrideCommand": true,

  "postCreateCommand": "bash -lc 'cd /home/node && node -v && npm -v && npm ci && echo \"🚀 Initial build...\" && WEBPACK_MODE=development ./smart-build.sh'",
  "postStartCommand": "bash -lc 'cd /home/node && echo \"🔧 Starting dev server via postStartCommand...\" && ./start-dev-server.sh && echo \"✅ postStartCommand completed\"'",

  "portsAttributes": {
    "8081": { "label": "App", "onAutoForward": "notify" }
  }
}
